# ============================================================
#  requirements.txt  —  Face-ID Attendance Kiosk
#
#  Local PC (scanner + dashboard):
#    pip install -r requirements.txt
#
#  Cloud dashboard only (no camera): use requirements_cloud.txt
#
#  Runs CPU-only. No GPU, torch, YOLO, ByteTrack or ReID needed —
#  the pipeline is: webcam -> InsightFace detect+recognise ->
#  MiniFASNet liveness -> MongoDB.
# ============================================================

# ── Vision ──────────────────────────────────────────────────
opencv-python>=4.9.0

# ── Face recognition (InsightFace ArcFace / RetinaFace) ─────
insightface>=0.7.3

# ── ONNX runtime — runs BOTH InsightFace and MiniFASNet liveness
onnxruntime>=1.17.0

# ── Data + database ─────────────────────────────────────────
numpy>=1.26.0,<2.0.0
pandas>=2.2.0
scikit-learn>=1.4.0
openpyxl>=3.1.0
pymongo>=4.6.0
dnspython>=2.0.0          # required for MongoDB Atlas mongodb+srv:// URIs

# ── Web dashboard ───────────────────────────────────────────
flask>=3.0.0
gunicorn>=20.1.0

# ── Utilities ───────────────────────────────────────────────
Pillow>=10.2.0
python-dotenv>=1.0.0
